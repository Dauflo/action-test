name: "Dauflo test"
description: "Trying to do things"
inputs:
  who-to-greet:
    description: "Who to greet"
    default: "World"
outputs:
  time:
    description: "The time we greeted you"

runs:
  using: "node12"
  main: "index.js"